 {% extends "base.html.twig" %}
    
    {% block body %}

        <div class="main-container">
            <div class="ajout-stage">
            
                <h1>Ajouter un stage</h1>

                {{form_start(form) }}
                    {{ form_widget(form) }}
                {{ form_end(form) }}      
            
            </div>
        </div>

    {% endblock %}